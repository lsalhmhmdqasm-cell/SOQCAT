# نظام White Label الآلي - دليل الاستخدام

## 🎯 نظرة عامة

هذا النظام يسمح لك ببناء تطبيقات مخصصة لكل محل **تلقائياً** دون تعب يدوي.

## 📁 البنية

```
copy-of-qatshop/
├── config/
│   └── shops/              # ملفات تكوين المحلات
│       ├── shop_001.json
│       ├── shop_002.json
│       └── ...
├── scripts/
│   ├── generate-config.js  # إنشاء تكوين محل جديد
│   ├── build-shop.js       # بناء تطبيق محل واحد
│   └── build-all.js        # بناء جميع التطبيقات
└── builds/                 # مخرجات APK
    ├── shop_001/
    │   ├── سوق_بن_عبود_v1.0.0.apk
    │   └── معلومات_التطبيق.txt
    └── shop_002/
        └── ...
```

## 🚀 الاستخدام

### 1. إضافة محل جديد

```bash
# طريقة تفاعلية (موصى بها)
node scripts/generate-config.js

# سيطلب منك:
# - اسم المحل
# - اسم المالك
# - رقم الهاتف
# - العنوان
# - اللون الأساسي
# الخ...
```

أو يمكنك إنشاء ملف JSON يدوياً في `config/shops/`:

```json
{
  "shopId": "shop_003",
  "shopName": "سوق صنعاء للقات",
  "ownerName": "خالد الصنعاني",
  "logo": "https://yourcdn.com/logos/sanaa.png",
  "primaryColor": "#dc2626",
  "phone": "771234567",
  "address": "صنعاء - شارع الزبيري",
  "appId": "com.qatshop.sanaa",
  "appName": "سوق صنعاء",
  "versionCode": 1,
  "versionName": "1.0.0"
}
```

### 2. بناء تطبيق محل واحد

```bash
# بناء محل معين
node scripts/build-shop.js shop_001

# سيقوم بـ:
# ✓ قراءة التكوين
# ✓ تحديث الألوان والشعار
# ✓ بناء Frontend
# ✓ بناء Android APK
# ✓ حفظ في builds/shop_001/
```

### 3. بناء جميع التطبيقات دفعة واحدة

```bash
# بناء كل المحلات
node scripts/build-all.js

# سيبني جميع المحلات في config/shops/
# ويعطيك تقرير نهائي
```

## ⚙️ كيف يعمل النظام

### 1. قراءة التكوين
السكريبت يقرأ ملف JSON للمحل ويستخرج:
- اسم التطبيق
- الشعار
- الألوان
- معلومات الاتصال

### 2. تحديث الملفات
يقوم بتحديث:
- `.env.local` - معرف المحل
- `shops-config.json` - التكوين الكامل
- `capacitor.config.ts` - App ID واسم التطبيق
- `manifest.json` - PWA manifest

### 3. البناء
- `npm run build` - بناء React
- `npx cap sync` - مزامنة Capacitor
- `./gradlew assembleRelease` - بناء APK

### 4. الإخراج
- APK جاهز للتوزيع
- ملف معلومات نصي
- كل شيء في مجلد منفصل

## 📱 تسليم التطبيق للعميل

بعد البناء، ستجد في `builds/shop_XXX/`:

1. **ملف APK** - جاهز للإرسال
2. **ملف معلومات** - يحتوي على:
   - اسم التطبيق
   - الإصدار
   - تاريخ البناء
   - معلومات الاتصال

### طرق التسليم:

#### أ. إرسال مباشر (الأسهل)
```bash
# أرسل APK عبر:
- واتساب
- تلجرام
- بريد إلكتروني
- Google Drive / Dropbox
```

#### ب. رفع على Google Play (احترافي)
1. إنشاء حساب مطور للعميل ($25 مرة واحدة)
2. رفع APK
3. ملء معلومات التطبيق
4. نشر

## 🔧 التخصيص المتقدم

### تغيير الألوان ديناميكياً

التطبيق يقرأ من `shops-config.json` ويطبق الألوان تلقائياً.

### إضافة شعار مخصص

1. رفع الشعار على CDN
2. تحديث `logo` في ملف التكوين
3. إعادة البناء

### تخصيص الميزات

يمكنك إضافة خصائص في التكوين:

```json
{
  "shopId": "shop_001",
  "features": {
    "enableDelivery": true,
    "enablePayment": false,
    "enableReviews": true
  }
}
```

ثم في الكود:

```typescript
const config = JSON.parse(localStorage.getItem('shopConfig'));
if (config.features.enableDelivery) {
  // عرض خيارات التوصيل
}
```

## 📊 إدارة المحلات

### عرض جميع المحلات

```bash
# في مجلد config/shops
ls -la

# أو
node -e "const fs=require('fs'); console.log(fs.readdirSync('config/shops'));"
```

### حذف محل

```bash
# احذف ملف التكوين
rm config/shops/shop_XXX.json

# احذف المخرجات
rm -rf builds/shop_XXX/
```

## 🎨 نصائح للإنتاجية

### 1. استخدم قالب Excel
أنشئ ملف Excel بكل معلومات المحلات:

| Shop ID | اسم المحل | المالك | الهاتف | العنوان | اللون |
|---------|-----------|--------|---------|---------|-------|
| shop_001 | بن عبود | محمد | 772519054 | مأرب | #15803d |
| shop_002 | الحديدة | أحمد | 777888999 | الحديدة | #1e40af |

ثم استخدم سكريبت لتحويله إلى JSON.

### 2. بناء ليلي
شغل `build-all.js` ليلاً واستيقظ على جميع التطبيقات جاهزة!

### 3. نظام تسمية
استخدم نظام تسمية واضح:
- `shop_001`, `shop_002` للمحلات
- `v1.0.0`, `v1.0.1` للإصدارات

## ⚠️ ملاحظات مهمة

### 1. Backend واحد مشترك
- جميع التطبيقات تتصل بنفس Backend
- كل محل له بيانات منفصلة في قاعدة البيانات
- يتم التمييز عبر `shopId`

### 2. التحديثات
عند تحديث التطبيق:
1. عدل الكود الأساسي
2. زد `versionCode` في ملفات التكوين
3. أعد بناء جميع التطبيقات
4. أرسل التحديثات للعملاء

### 3. الشعارات
- استخدم CDN لاستضافة الشعارات
- الحجم الموصى به: 512x512 بكسل
- الصيغة: PNG بخلفية شفافة

### 4. الألوان
- استخدم Hex Colors (#rrggbb)
- اختر ألوان متناسقة
- اختبر على خلفيات فاتحة وداكنة

## 🐛 حل المشاكل

### المشكلة: فشل البناء
```bash
# تأكد من تثبيت المكتبات
npm install

# تأكد من وجود Android SDK
echo $ANDROID_HOME

# نظف وأعد البناء
cd android
./gradlew clean
cd ..
node scripts/build-shop.js shop_001
```

### المشكلة: APK لا يعمل
- تأكد من توقيع APK (للإنتاج)
- تأكد من صلاحيات Android

### المشكلة: الشعار لا يظهر
- تأكد من رابط الشعار صحيح
- تأكد من الشعار متاح عبر HTTPS
- جرب فتح الرابط في المتصفح

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع هذا الدليل
2. تحقق من ملفات التكوين
3. راجع رسائل الخطأ
4. اتصل بالمطور

---

**نصيحة أخيرة:** احفظ نسخة احتياطية من `config/shops/` دائماً! 🔒
